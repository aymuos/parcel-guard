import { AppShell, PageHeader } from "@/components/AppShell";
export default function TerminalsPage() { return <AppShell><div className="content"><PageHeader eyebrow="Network intelligence" title="Terminals" description="Terminal-level risk data will appear here when the backend exposes terminal aggregation." /><div className="panel"><div className="empty-state"><strong>No terminal aggregation endpoint detected.</strong><br />The current API exposes parcel-level prediction and fleet-level ROI. This view is intentionally ready for the planned <code>/admin/terminal-risk</code> contract.</div></div></div></AppShell>; }
