"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { AppShell } from "@/components/AppShell";
import { SAMPLE_TRACKING_ID } from "@/lib/api";

export default function TrackPage() { const router = useRouter(); const [trackingId, setTrackingId] = useState(""); const submit = (event: FormEvent) => { event.preventDefault(); const id = trackingId.trim() || SAMPLE_TRACKING_ID; router.push(`/track/${encodeURIComponent(id)}`); }; return <AppShell customer><main className="track-wrap"><div className="track-intro"><p className="eyebrow">Parcel Promise Guard</p><h1>Track your parcel.</h1><p>See when your parcel is expected, understand delays, and explore delivery options.</p></div><form className="track-form" onSubmit={submit}><input aria-label="Tracking ID" placeholder="Enter tracking ID" value={trackingId} onChange={event => setTrackingId(event.target.value)} /><button type="submit">Track parcel →</button></form><p className="demo-note">Try the demo parcel <a href={`/track/${SAMPLE_TRACKING_ID}`}>{SAMPLE_TRACKING_ID}</a></p><div className="track-features"><div className="feature"><span className="feature-mark">01</span><b>Know the moment</b><span>Clear delivery windows backed by live prediction.</span></div><div className="feature"><span className="feature-mark">02</span><b>Understand the why</b><span>Plain-language explanations when the promise is at risk.</span></div><div className="feature"><span className="feature-mark">03</span><b>Choose what happens next</b><span>Recovery actions where the delivery network supports them.</span></div></div></main></AppShell>; }
